# 聊天模型配置
# CHAT_PROVIDER 可选: openai, deepseek, qwen (默认根据模型名自动识别)
# CHAT_PROVIDER=openai
OPENAI_API_KEY=your_chat_api_key
OPENAI_API_BASE=https://api.openai.com/v1
OPENAI_MODEL=gpt-4.1-mini

# 如果你用 DeepSeek 的 OpenAI 兼容接口，可改成下面这种：
# OPENAI_API_KEY=your_deepseek_api_key
# OPENAI_API_BASE=https://api.deepseek.com
# OPENAI_MODEL=deepseek-chat

# 如果你用通义千问（Qwen），可改成下面这种：
# DASHSCOPE_API_KEY=your_dashscope_api_key
# OPENAI_MODEL=qwen-max
# 或直接指定提供者：
# CHAT_PROVIDER=qwen

# Embedding 配置
OPENAI_EMBEDDING_API_KEY=
OPENAI_EMBEDDING_API_BASE=https://api.openai.com/v1
OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# 视觉模型配置（用于 MarkItDown OCR，处理 PDF/图片中的内容）
# 如果不配置，MarkItDown 使用基础模式（不提取图片中的文字）
# 配置后自动启用 LLM Vision OCR
VISION_API_KEY=your_siliconflow_api_key
VISION_BASE_URL=https://api.siliconflow.cn/v1
VISION_MODEL=zai-org/GLM-4.6V

# 代理配置 (不再在代码中显式对齐，如有需要请使用全局环境变量 HTTP_PROXY)
# HTTP_PROXY=http://127.0.0.1:7890
# HTTPS_PROXY=http://127.0.0.1:7890
# NO_PROXY=localhost,127.0.0.1

# RAG 配置
KNOWLEDGE_DIR=data/knowledge
VECTOR_STORE_PATH=.cache/vector_store.json
RAG_TOP_K=4
RAG_CHUNK_SIZE=400
RAG_CHUNK_OVERLAP=80

# 和风天气配置
QWEATHER_PROJECT_ID=your_qweather_project_id
QWEATHER_KEY_ID=your_qweather_key_id
# 填写 Ed25519 私钥文件路径
QWEATHER_PRIVATE_KEY_PATH=
# 使用控制台中分配的专属 API Host，例如 https://abcxyz.qweatherapi.com
QWEATHER_API_HOST=https://your-api-host.qweatherapi.com
QWEATHER_JWT_TTL_SECONDS=900
WEATHER_LANG=zh
WEATHER_UNIT=m
WEATHER_FORECAST_DAYS=3
WEATHER_TIMEOUT_SECONDS=10
