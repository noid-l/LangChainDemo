# LangChain 基础说明

LangChain 是一个用于构建 LLM 应用的开发框架。它把提示词、模型调用、检索、工具调用和工作流组合能力封装成统一接口，方便开发者快速搭建问答、Agent、RAG 和自动化流程。

在一个最小示例中，开发者通常会先定义 PromptTemplate，再选择一个聊天模型，例如 OpenAI 的 `gpt-4.1-mini`，最后把提示词和模型串成一条可复用链路。

LangChain 的价值不在于隐藏模型能力，而在于把应用级的工程问题组织起来，例如输入输出结构、上下文拼装、可观测性和组件替换。
